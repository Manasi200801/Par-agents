# Secrets — never commit
.env
.env.local
.env.*.local
*.key
.claude/settings.local.json

# OS junk
.DS_Store

# Node
node_modules/
.next/
out/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Database (generated at runtime)
data/*.duckdb
data/*.duckdb.wal

# Firn data is out of scope
data/firn-outdoor-ag/
# Alpine forecast variant — not used (we use gmbh)
data/alpine-manufacturing-forecast/
